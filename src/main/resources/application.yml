spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5433/intruder}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:root}

  jpa:
    show-sql: true
    generate-ddl: true

  freemarker:
    expose-request-attributes: true
    suffix: .ftl

  # Настройки почты
  mail:
    host: smtp.yandex.ru
    username: application.web@yandex.ru
    password: VdI4k8lxhOJdQ149O7ZT
    port: 465
    protocol: smtps

# На какой хост переходить для подтверждения регистрации
activation:
  host: "http://localhost:8080"

mail.debug: true

# Путь к фотографиям аварий
upload:
  path: "C:/Users/user/IdeaProjects/car-intruder/external/car-acident"
  avatar: "C:/Users/user/IdeaProjects/car-intruder/external/avatar"
